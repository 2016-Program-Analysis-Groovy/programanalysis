/* Testing array handling with negative index. Currently not working as intended in detection of signs analysis */
{
    int x;
    int a[2];
    x = -3;
    a[x] = a[x];
    if (x == a[x]) {
        write x;
    }
}